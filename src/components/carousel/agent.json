{
  "schema": "flowstack.agent-component.v1",
  "id": "carousel",
  "name": "Carousel",
  "package": "@flowstack-ui/brick",
  "layer": "brick",
  "kind": "component",
  "purpose": "Present one item or campaign at a time with optional navigation, picker dots, touch scrolling, and user-controlled rotation.",
  "useWhen": [
    "Several peer items benefit from sharing one bounded visual region.",
    "A campaign hero needs multiple authored messages without placing them all in the initial viewport."
  ],
  "avoidWhen": [
    { "condition": "Every item must be visible for comparison or comprehension.", "useInstead": "Grid, Stack, List, or Tabs" },
    { "condition": "The content is only decorative image rotation.", "useInstead": "a non-interactive media treatment that respects reduced motion" }
  ],
  "composition": [
    "Compose Root, Viewport, Track, and one Slide per unique value; add Navigation, Controls, Previous, Next, Picker, PickerItem, and RotationControl only when the product needs them.",
    "In a React Server Component, import the subpath as a module namespace with import * as Carousel from '@flowstack-ui/brick/carousel'; use the frozen Carousel runtime object only inside client components.",
    "When automatic rotation is enabled, always render RotationControl, Previous, and Next so people can stop and navigate the sequence; place RotationControl before Viewport in DOM order.",
    "Build campaign-specific slide content from Brick layout, typography, action, and media components; Carousel does not own hero messaging or proof content.",
    "When artwork, scrim, and copy form one peer campaign, compose a complete Surface with Media, Scrim, and Content inside each Slide; keep genuinely invariant evidence or navigation outside the rotating sequence.",
    "When a parent already owns a stable block size, use Root fill to propagate it through Carousel's Viewport, Track, and Slides; continue sizing authored Surface, Content, and layout components explicitly."
  ],
  "rules": [
    { "id": "carousel-optional-controls", "level": "must", "statement": "Treat arrows and picker dots as optional authored controls; dots represent direct slide selection and are not Tabs." },
    { "id": "carousel-autoplay-control", "level": "must", "statement": "Never enable automatic rotation without a visible RotationControl and direct Previous and Next controls." },
    { "id": "carousel-control-independence", "level": "must", "statement": "Keep picker treatment independent from arrow visibility; use Picker variant bare for dots without a capsule and Navigation visibility interaction only when arrows remain discoverable through focus, pointer, and touch." },
    { "id": "carousel-quiet-rotation-control", "level": "should", "statement": "Use a compact xs ghost RotationControl when the required stop mechanism should remain visually quiet; never remove it while automatic rotation can run." },
    { "id": "carousel-loop-direction", "level": "must", "statement": "Preserve requested direction at loop boundaries and never clone authored slide content, IDs, controls, or form fields." },
    { "id": "carousel-initial-motion-gate", "level": "must", "statement": "Keep viewport motion instant until Atom exposes data-initialized; Brick's shipped Carousel CSS already enables smooth motion only after that signal." },
    { "id": "carousel-stable-geometry", "level": "should", "statement": "Give campaign slides stable responsive geometry so changing the active slide does not move surrounding page content." },
    { "id": "carousel-fill-ownership", "level": "must", "statement": "Treat fill as internal size propagation, not a viewport-height policy; the application or Block must establish the available parent height." },
    { "id": "carousel-overlay-safe-area", "level": "must", "statement": "When controls overlay slides, reserve application-owned content safe areas so arrows, rotation controls, and picker targets never obscure authored text or actions." },
    { "id": "carousel-media-loading", "level": "should", "statement": "Prioritize only initially visible campaign media and defer non-current media when the image delivery layer supports it." },
    { "id": "carousel-css", "level": "must", "statement": "Load styles.css or core.css plus carousel.css." }
  ],
  "commonMistakes": [
    { "mistake": "Using Tabs for picker dots or hiding the only way to stop autoplay.", "correction": "Use PickerItem buttons and render RotationControl whenever autoplay can run." },
    { "mistake": "Putting unrelated proof rails or page sections inside every hero slide.", "correction": "Rotate only the peer campaign content and keep stable page evidence outside the Carousel." },
    { "mistake": "Rotating campaign copy while leaving campaign-specific artwork behind as one unrelated background.", "correction": "Decide whether media is invariant or part of the campaign; when it communicates that campaign, place the complete Surface inside Slide." },
    { "mistake": "Repeating block-size rules across Carousel Viewport, Track, and Slide.", "correction": "Establish the parent height once and opt into Root fill; size only the authored content inside each Slide separately." }
  ],
  "validation": [
    "Check optional-control compositions, first and last boundaries in both directions, interaction-only arrow discovery, independent picker treatment, native horizontal touch scrolling, focus pause, hover pause, reduced motion, RTL, and screen-reader naming.",
    "Confirm inactive slides are unavailable to focus and assistive technology, control focus rings remain visible, overlay controls do not cover content, complete campaign media changes with its copy, and slide changes do not cause page-level layout shift."
  ],
  "related": ["button", "icon-button", "image", "surface", "stack", "grid", "text", "tabs"]
}
