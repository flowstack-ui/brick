{
  "schema": "flowstack.agent-component.v1",
  "id": "bleed",
  "name": "Bleed",
  "package": "@flowstack-ui/brick",
  "layer": "brick",
  "kind": "component",
  "purpose": "Extend authored content across a parent inset with responsive logical spacing.",
  "useWhen": ["Edge media or artwork must cross its immediate container padding."],
  "avoidWhen": [{ "condition": "The problem is size, paint, ordinary arrangement, or unrelated positioning.", "useInstead": "Frame, Surface, Stack, Grid, or application layout" }],
  "composition": ["Place Bleed inside the owner of the inset it crosses and keep meaningful media or content semantics on the authored child."],
  "rules": [
    { "id": "bleed-positive-input", "level": "must", "statement": "Use public non-negative spacing values; Bleed owns the negative conversion." },
    { "id": "bleed-axis-first", "level": "should", "statement": "Prefer inline or block for an axis and directional props only for a genuine one-edge exception." },
    { "id": "bleed-verify", "level": "must", "statement": "Verify narrow widths, RTL, zoom, and horizontal overflow." }
  ],
  "commonMistakes": [{ "mistake": "Using Bleed to fix an incorrect Container measure.", "correction": "Correct the owning layout first." }],
  "validation": ["Confirm the intended edges align with the parent's outer boundary.", "Confirm the page does not gain horizontal overflow."],
  "related": ["frame", "stack", "surface", "image"]
}
