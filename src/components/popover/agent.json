{
  "schema": "flowstack.agent-component.v1",
  "id": "popover",
  "name": "Popover",
  "package": "@flowstack-ui/brick",
  "layer": "brick",
  "kind": "component",
  "purpose": "Present a compact click-open panel anchored to a trigger while Atom owns placement, focus, dismissal, and portal behavior.",
  "useWhen": [
    "A trigger needs a small contextual panel for details, filters, or compact editing that does not block the rest of the page."
  ],
  "avoidWhen": [
    {
      "condition": "The content is short noninteractive help, a hover preview, or a task that must create a modal boundary.",
      "useInstead": "Tooltip, HoverCard, or Dialog according to the interaction"
    }
  ],
  "composition": [
    "Compose Root > Trigger plus Portal > Content; place Title and Description when they clarify the panel, arrange authored content through Header, Body, Footer, and Close as needed, and use density=compact for concise utility panels.",
    "When a local Appearance scope owns the trigger, portal into that scope or apply the same Appearance to Content because ordinary portal ancestry does not preserve the local theme boundary."
  ],
  "rules": [
    {
      "id": "popover-density",
      "level": "must",
      "statement": "Use the comfortable default for ordinary contextual work and density=compact for concise notification, filter, and utility panels; do not recreate compact rhythm with private selectors."
    },
    {
      "id": "popover-click-context",
      "level": "must",
      "statement": "Use Popover for compact nonmodal click-open work; do not use it for a blocking workflow or as a substitute for persistent page content."
    },
    {
      "id": "popover-complete-anatomy",
      "level": "must",
      "statement": "Keep Trigger and portalled Content inside one Root and use the public structure parts instead of recreating panel spacing and hierarchy with private selectors."
    },
    {
      "id": "popover-theme-scope",
      "level": "must",
      "statement": "When Content leaves a local appearance or theme scope through Portal, reproduce that scope on the portalled visual root or target a container inside it."
    },
    {
      "id": "popover-accessible-context",
      "level": "must",
      "statement": "Give the trigger a complete accessible name and include Title or Description when the panel purpose would otherwise be unclear."
    },
    {
      "id": "popover-surface-integrity",
      "level": "must",
      "statement": "Keep Content and Arrow on the finished Popover surface contract so they share background and border paint; do not independently recolor or offset the Arrow."
    },
    {
      "id": "popover-css",
      "level": "must",
      "statement": "Load styles.css or core.css plus popover.css."
    }
  ],
  "commonMistakes": [
    {
      "mistake": "Hand-positioning a floating div, assuming a portalled panel inherits a nearby theme scope, or putting a modal task in Popover.",
      "correction": "Use complete Popover anatomy, make the portal scope explicit, and choose Dialog when focus must remain inside the task."
    }
  ],
  "validation": [
    "Check trigger naming, open and close behavior, focus return, Escape and outside dismissal, centered and edge collision placement, Arrow attachment at every supported radius, zoom, narrow viewports, and RTL.",
    "Check light and dark appearance on the actual portalled Content and confirm the matching CSS is loaded."
  ],
  "related": [
    "dialog",
    "hover-card",
    "tooltip",
    "appearance",
    "stack"
  ]
}
