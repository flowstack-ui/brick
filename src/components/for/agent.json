{
  "schema": "flowstack.agent-component.v1",
  "id": "for",
  "name": "For",
  "package": "@flowstack-ui/brick",
  "layer": "brick",
  "kind": "component",
  "purpose": "Render a typed collection or one explicit fallback without inserting a wrapper.",
  "useWhen": ["An authored collection renders repeated JSX while keeping its collection owner and item identity explicit."],
  "avoidWhen": [{"condition": "The operation transforms, filters, groups, or aggregates data rather than rendering JSX.", "useInstead": "native array methods or the domain collection owner"}],
  "composition": ["Place For inside a semantic List, Grid, Stack, Select, MultiSelect, or other authored owner and put a stable key on each returned item root. Do not place it directly inside an owner such as AvatarGroup that must count and wrap its immediate authored children."],
  "rules": [
    {"id": "for-keys", "level": "must", "statement": "Author stable keys on returned item roots; For does not infer identity."},
    {"id": "for-wrapper", "level": "must", "statement": "Do not add a wrapper merely for For; choose the semantic collection owner separately."},
    {"id": "for-css", "level": "must", "statement": "Load styles.css or core.css plus for.css."}
  ],
  "commonMistakes": [{"mistake": "Using the source index as identity for reorderable data.", "correction": "Use the item's stable domain identifier as the returned root key."}, {"mistake": "Placing For directly inside a component that counts or transforms immediate authored children, such as AvatarGroup.", "correction": "Author the fixed children explicitly or use that owner's collection API so its direct-child contract remains visible."}],
  "validation": ["Verify direct child structure, item order, empty and undefined fallback, inferred item types, and key ownership."],
  "related": ["list", "grid", "stack", "select", "multi-select", "avatar-group"]
}
