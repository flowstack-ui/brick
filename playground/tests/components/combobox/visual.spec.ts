import { expect, installVisualDefaults, setAppearance, test, useForcedColors } from "../../visual-harness.js";
installVisualDefaults("/combobox");
test("Combobox recipes and states", async ({ page }) => { await expect(page.locator('[data-scenario="combobox.overview"]')).toHaveScreenshot("overview-light.png"); await expect(page.locator('[data-scenario="combobox.recipes"]')).toHaveScreenshot("recipes-light.png"); await expect(page.locator('[data-scenario="combobox.states"]')).toHaveScreenshot("states-light.png"); });
test("Combobox dark, mobile RTL, and forced colors", async ({ page }) => { await setAppearance(page,"dark"); await expect(page.locator('[data-scenario="combobox.appearance"]')).toHaveScreenshot("appearance-dark.png"); await page.setViewportSize({width:390,height:844}); await expect(page.locator('[data-scenario="combobox.stress"]')).toHaveScreenshot("stress-mobile.png"); await page.setViewportSize({width:1120,height:900}); await useForcedColors(page); await expect(page.locator('[data-scenario="combobox.states"]')).toHaveScreenshot("states-forced-colors.png"); });
