import { expect,installVisualDefaults,setAppearance,test,useForcedColors } from "../../visual-harness.js";
installVisualDefaults("/slider");
test("Slider anatomy, recipes, states, direction, and theme",async({page})=>{for(const id of ["slider-overview","slider-values","slider-recipes","slider-states","slider-direction","slider-content"])await expect(page.getByTestId(id)).toHaveScreenshot(`${id.replace("slider-","")}-light.png`);await setAppearance(page,"dark");await expect(page.getByTestId("slider-appearance")).toHaveScreenshot("appearance-dark.png");await expect(page.getByTestId("slider-customization")).toHaveScreenshot("customization-dark.png")});
test("Slider mobile and forced colors",async({page})=>{await page.setViewportSize({width:390,height:844});await expect(page.getByTestId("slider-stress")).toHaveScreenshot("stress-mobile.png");await page.setViewportSize({width:1120,height:900});await useForcedColors(page);await expect(page.getByTestId("slider-states")).toHaveScreenshot("states-forced-colors.png")});
