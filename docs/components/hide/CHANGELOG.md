# Hide changelog

Hide follows the package version of `@flowstack-ui/brick`.

## Unreleased

### Added

- Added public Agent Knowledge for component selection, composition,
  CSS-delivery, recurring mistakes, and validation.

- Initial CSS-only `Hide` API with fixed `from` breakpoints, semantic native hosts, natural visible display, and always-mounted children.
