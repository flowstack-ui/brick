# Format Byte changelog

Format Byte follows the package version of `@flowstack-ui/brick`.

## Unreleased

### Added

- Added provider-aware bit and byte formatting with decimal or binary scaling and localized units.
