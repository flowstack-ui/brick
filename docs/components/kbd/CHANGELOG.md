# Kbd changelog

Kbd follows the package version of `@flowstack-ui/brick`.

## Unreleased

### Added

- Added Kbd with native keyboard-input semantics, four visual variants, three sizes, public CSS variables, and forced colors.
