# Format Number changelog

Format Number follows the package version of `@flowstack-ui/brick`.

## Unreleased

### Added

- Added provider-aware number formatting through a native span component and cached string helper.
