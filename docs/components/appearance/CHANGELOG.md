# Appearance changelog

Appearance follows the package version of `@flowstack-ui/brick`.

## Unreleased

### Added

- Added `Appearance`, one server-safe native or wrapper-free semantic boundary
  with explicit portal composition and no runtime theme provider.
