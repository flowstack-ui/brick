# Component name changelog

This component follows the package version of `@flowstack-ui/brick`.

## Unreleased

### Added

- Initial public component contract.

Record user-visible API, CSS, DOM, token, accessibility, responsive, and visual
changes here. Documentation-only maintenance does not create a component
version.
