# Skip Link changelog

Skip Link follows the package version of `@flowstack-ui/brick`.

## Unreleased

### Added

- Added `SkipLink.Root` and `SkipLink.Target` for a finished focus-reveal
  bypass link paired with Atom-owned native focus and fragment behavior.

